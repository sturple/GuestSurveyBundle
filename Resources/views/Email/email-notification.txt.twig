{{config.email.text.title}}
{{config.email.text.subtitle}}
{% if item.roomNumber | default(false) %}Room # {{item.roomNumber}}{% endif %}
{% if item.client_name | default(false) %}Name {{item.client_name }}{% endif %}
{% if item.client_email | default(false) %}Name {{item.client_email }}{% endif %}
Date {{item.createDate|default('now')|date('M d, Y')}}
Time {{item.createDate|default('now')|date('H:i')}}
Question: {{item.title|default('No Question')|raw}}
{% if data.answer|default(0) > 0 %}
Message: {{item.answer |default('--answer missing--')}}
{% else %}
Response: {{item.answer| default('--answer missing--')}} {%if item.type == 'rating'%}out of 5{%endif%}
{% endif %}

{{property.name}}
{{property.contact.address}}, {{property.contact.province}}, {{property.contact.country}}
{{property.contact.phone}}| TF: {{property.contact.tollfree}} | {{property.contact.email}} | {{property.contact.website}}
